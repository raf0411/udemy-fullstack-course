const intern = {
  name: "John",
  age: 30,
  company: "PT. Kalbe Farma tbk",
};

function getInternDetails(intern) {
  return `${intern.name} is ${intern.age} years old and works at ${intern.company}.`;
}